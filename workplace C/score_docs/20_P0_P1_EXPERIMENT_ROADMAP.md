# P0/P1 Experiment Roadmap

Generated: 2026-07-09T15:34:05

Roadmap: scan artifacts first, then run direct ONNX compression only where no public/local artifact beats current cost.

| task | priority | first experiment | second experiment | third experiment |
| --- | --- | --- | --- | --- |
| task158 | P0_lt16 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task286 | P0_lt16 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task054 | P0_lt16 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task364 | P0_lt16 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task349 | P0_lt16 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task077 | P0_lt16 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task096 | P1_16_16p7 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task009 | P1_16_16p7 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task383 | P1_16_16p7 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task382 | P1_16_16p7 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task278 | P1_16_16p7 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task165 | P1_16_16p7 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task378 | P1_16_16p7 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |
| task132 | P1_16_16p7 | full-validate top artifact candidates | inspect memory/params dominant cost | try graph simplify / constant pruning if artifact scan fails |

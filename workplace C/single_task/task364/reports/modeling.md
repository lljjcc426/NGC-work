# task364 independent modeling

The task performs barrier-preserving connected propagation from a component
seed. The independent candidate removes propagation rounds four and five and
classifies the third-round state directly.

It passes 199/266 examples. Larger one-shot pooling kernels cross barriers, so
the last iterations are semantically necessary. This structural experiment is
separate from the accepted two-cost Pad constant rewrite.
